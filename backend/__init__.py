"""FastAPI backend for StateTaxationAdminCrawler.

Exposes a REST + SSE API so crawled policies and DeepSeek reports (already pushed
to WeChat by the crawler) are also stored and displayed in a Vue frontend.
"""
