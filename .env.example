# Copy to `.env` and fill in. docker-compose.yml reads these at start.
# Values here are passed into the container as environment variables; secrets
# never get baked into the image.

# --- WeChat push (personal WeChat) ---
# Backend kind: serverchan | pushplus | wecom | generic
NOTIFY_WEBHOOK_KIND=serverchan
# Your SendKey from Server酱 (https://sctapi.ftqq.com) or pushplus (https://www.pushplus.plus)
NOTIFY_WEBHOOK_TOKEN=
# Only required for the `wecom` (企业微信机器人) or `generic` kinds:
NOTIFY_WEBHOOK_URL=

# --- Optional corporate HTTP proxy (leave empty if none) ---
PROXY_POOL=
PROXY_API_URL=

# --- DeepSeek API (scripts/generate_report.py 用它基于 .jsonl 原文生成报告摘要) ---
# 申请地址: https://platform.deepseek.com
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# --- Local FastAPI backend ---
# Optional: when omitted, a random owner-only token is generated in data/.
BACKEND_INGEST_TOKEN=
BACKEND_INGEST_URL=http://127.0.0.1:8000
DB_URL=sqlite:///./data/backend.db
CORS_ORIGINS=https://www.luck3mc.com,https://luck3mc.minecraftaoding.workers.dev,http://localhost:5200
