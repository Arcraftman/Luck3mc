# Scrapy deployment configuration.
# `default` points to the settings package; the active environment
# (development / production / testing) is selected via the SCRAPY_ENV
# environment variable (see crawler/settings/__init__.py).

[settings]
default = crawler.settings

[deploy]
project = StateTaxationAdminCrawler
